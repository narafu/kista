spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/kistadb
    username: kista
    password: kista
    driver-class-name: org.postgresql.Driver

  flyway:
    enabled: true

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate

logging:
  level:
    org.hibernate.SQL: DEBUG
